@using FundingPlatform.Web.ViewModels.Admin
@model AdminUserResetPasswordViewModel
@{
    ViewData["Title"] = "Reset password";
}

<div data-testid="admin-area">
    @await Html.PartialAsync("Components/_PageHeader",
        new PageHeaderViewModel(
            Title: $"Reset password — {Model.TargetEmail}",
            Subtitle: "The user will be required to change this temporary password on next sign-in.",
            Breadcrumbs: new[]
            {
                new BreadcrumbItem("Users", Url.Action("Index", "AdminUsers")),
                new BreadcrumbItem("Reset password"),
            }))

    <form method="post" asp-action="ResetPassword" asp-route-id="@Model.UserId" class="row" data-testid="admin-user-reset-form">
        @Html.AntiForgeryToken()
        <input asp-for="UserId" type="hidden" />
        <input asp-for="TargetEmail" type="hidden" />
        <div asp-validation-summary="All" class="text-danger mb-3"></div>

        <div class="col-md-6 mb-3">
            <label class="form-label" asp-for="NewTemporaryPassword">New temporary password</label>
            <input asp-for="NewTemporaryPassword" class="form-control" type="password" autocomplete="new-password" />
            <span asp-validation-for="NewTemporaryPassword" class="text-danger"></span>
        </div>
        <div class="col-md-6 mb-3">
            <label class="form-label" asp-for="ConfirmPassword">Confirm new password</label>
            <input asp-for="ConfirmPassword" class="form-control" type="password" autocomplete="new-password" />
            <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-end gap-2 mt-2">
            <a class="btn btn-secondary" href="@Url.Action("Index", "AdminUsers")">Cancel</a>
            <button type="submit"
                    class="btn btn-warning"
                    data-testid="admin-user-reset-submit"
                    onclick="return confirm('Reset password for @Model.TargetEmail? They will be signed out immediately.');">
                Reset password
            </button>
        </div>
    </form>
</div>
