/* canvas-confetti shim — placeholder.
   The real minified library MUST replace this file (≤ 5 KB gz, ISC).
   Until then, this shim exposes a no-op so _SigningCeremony's call site
   does not throw when the file is loaded. */
(function (root) {
  function noop() { return Promise.resolve(); }
  noop.create = function () { return noop; };
  noop.reset = function () { };
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = noop;
  } else {
    root.confetti = noop;
  }
})(typeof window !== 'undefined' ? window : globalThis);
